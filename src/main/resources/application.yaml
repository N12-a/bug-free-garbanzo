spring:
  # ?????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/farm_product?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: 1190

  # Redis??
  data:
    redis:
      host: localhost
      port: 6379
      password:
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          min-idle: 1
          time-between-eviction-runs: 10s

  # ?????
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
  # ???????????????
  mail:
    host: smtp.qq.com
    port: 587
    username: ??QQ??@qq.com
    password: ??QQ?????
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

# MyBatis-Plus??
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.farm.product.entity
  configuration:
    map-underscore-to-camel-case: true


# ?????
server:
  port: 8080 # ????

# JWT 自定义配置（生产环境需替换为复杂密钥）
jwt:
  secret: farm-product-system-2025-secret-key-1234567890  # 至少32位随机字符串
  expire: 86400000  # token有效期：24小时（毫秒）
  header: Authorization  # 请求头中token的key
  prefix: Bearer  # token前缀（前端请求拦截器需匹配）